# Safe for version control — contains only public API URLs, no secrets.
# The VITE_API_URL value is the publicly deployed backend and does not
# contain any credentials, tokens, or sensitive information.
VITE_API_URL=https://carbon-production-49fd.up.railway.app/api
